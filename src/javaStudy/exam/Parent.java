package javaStudy.exam;

// Parent 클래스를 상속해서 Child 클래스를 다음과 같이 작성했는데, Child 클래스의 생성자에서 컴파일 에러가 발생하였다.
// 이유를 설명해봐라.
public class Parent {
    public String name;

    public Parent(String name) {
        this.name = name;
    }
}
